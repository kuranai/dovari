/**
 * Worker boundary reserved for phase P01.
 *
 * Cloudflare bindings, Hono routing, and request handling intentionally do not
 * belong to the P00 project scaffold.
 */
export const workerScaffold = 'p01-boundary';
